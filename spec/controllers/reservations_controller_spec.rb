require 'spec_helper'

describe ReservationsController do
	describe "POST create" do
		context "during creation of a group" do
			it "sets the student_id to the id of the newly created student"
			it "sets the group_id to the id of the newly created group"
		end
	end
end